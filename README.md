# banking-text-embedding
Domain-specific word embeddings for Banking and Retail Finance using CBOW and Skip-gram models implemented in Python with Keras. The project builds custom embeddings from a banking corpus using one-hot encoding and evaluates semantic similarity with cosine similarity.
